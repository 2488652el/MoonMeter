# 更新日志

## Unreleased

## 1.3.0 - 2026-07-28

- 新增 Windows/WSL 来源健康中心：首次只读发现 WSL 发行版，不启动发行版或扫描日志；解析 `$HOME`、预览候选目录、启用来源和同步均为显式动作，支持 Claude Code、Codex、Kimi Code、Gemini CLI 与 OpenCode。
- 新增项目用量聚合与任务归属：由 Main/SQLite 汇总成本、Token、会话和 Git numstat；Git 只读且超时 fail-soft；会话、commit 和手动 HTTPS PR 需用户确认后才归属任务。
- 新增 90 天详细事件时间线、游标分页、状态/类型/时间筛选和旧详情按日聚合；新增默认关闭的本地回环 OTLP/HTTP JSON 接收器，Bearer Token 使用 safeStorage 保存并执行字段、大小、速率和去重限制。
- 新增默认关闭的独立 mini panel，可选固定项目与全局快捷键；快捷键冲突会安全降级为关闭，不自动修改外部工具配置。详见 [本地数据隐私边界](./design/LOCAL_DATA_PRIVACY.md)。

## 1.2.55 - 2026-07-28

- 优化 API Keys 的删除确认与连接测试反馈：改为应用内二级弹窗，统一圆角、色彩、动效与按钮层级；测试过程中和成功/失败结果均在弹窗内清晰呈现。

## 1.2.54 - 2026-07-27

- 调整首页信息架构：先选择统计范围并查看核心指标，再处理行动提醒；额度规划、软预算、周期报告和来源状态归入分析内容之后的“规划与管理”区。
- 行动中心现在会标明需要处理的 Provider / 账户别名，不再重复展示无法区分来源的通用刷新提示。

- 优化 API Keys 首次打开性能：本机会话统计改由主进程聚合后返回，避免将大量原始请求记录传入渲染进程造成卡顿；展示的请求、Token、会话和模型统计保持不变。

## 1.2.53 - 2026-07-27

- 新增只读账户身份面板：同一 Provider 下的 Key、本地来源、工作区和同步设备独立显示，并支持本机别名与排序；不会合并余额、额度或成本。

- 新增本地周报/月报：按当前账期已过时长与前一同期比较，汇总 Provider、模型、项目与高成本会话；说明发生时间、估算/未计价/未折算缺口，并可关闭报告或导出脱敏 CSV。
- 新增透明使用建议：仅在精确相同模型且存在可比较的已配置价格时，按当前周期实际 token 结构试算候选成本；可关闭，不会自动排序或切换 Provider。
- 新增本地脱敏诊断包预览和 JSON 下载：仅包含版本、来源状态、错误码、计价覆盖和数据量；不上传且不包含凭据、账号别名、路径、Prompt 或原始错误。
- 设置页新增反馈与功能投票入口：在系统浏览器中由用户自行提交，不自动上传诊断包或行为数据。

- 新增首次激活与额度规划 MVP：统一 Codex、Kimi 与 MiniMax 周期额度，持久化本地脱敏样本并在至少 3 个有效观测后给出失败关闭的速度预测；首页新增来源健康、24 小时计价覆盖和最多 3 条可追溯行动建议。
- 本地 Session 解析新增 Gemini CLI 与 OpenCode：按官方本地记录格式导入 token、缓存 token 与 OpenCode 成本，统一纳入来源健康和已有会话统计。
- 新增本地软预算：支持自然月或自定义循环账期的总额、Provider、项目预算；按 50%/80%/100% 阶梯记录本地提醒，展示账期筛选、发生时/估算成本覆盖、不可折算币种与当前速度预测，不会阻断请求。

## 1.2.52 - 2026-07-25

- 用量告警接入 Windows/macOS 原生通知，新增事件历史、已读状态、Provider 跳转、规则编辑和通知送达状态；只有系统确认展示后才标记成功，原生失败会保留错误；连续两次越界才会送达，同一异常只触发一次，恢复后再次越界才会重发。
- 新入库请求保存供应商成本或发生时价格快照，后续价格目录变化不再改写历史成本；旧记录在界面与 CSV 中明确标记为“当前价格估算”。
- 新增“本月至今”和自定义账期，并让首页、Provider 汇总、模型对比与请求日志复用时间、来源、模型和项目筛选口径；四个页面使用同一汇率上下文折算人民币。

## 1.2.51 - 2026-07-25

- 修复 API Keys 页 Codex CLI 卡片在深色主题下的配色：额度图标、下次重置与错误提示改用主题令牌（accent / 状态色），不再使用浅色底块，与整体设计风格一致。
- 修复用量概览“缓存命中率”进度条在深色主题下几乎不可见的问题：改用高对比度成功绿色与标准轨道色，百分比图形可正确显示。

## 1.2.5 - 2026-07-24

- 价格目录每个模型只显示一个价格：同一模型被官方与聚合商同时收录时保留官方价，官方目录没有的聚合商独有模型仍保留。

## 1.2.4 - 2026-07-24

- 首页重构为紧凑的两行核心指标卡片，并新增来源、模型、项目/Agent 统一筛选；成本、Token、请求数、趋势和计价覆盖均按真实查询结果联动更新。
- 请求日志复用首页筛选状态，新增项目/Agent 服务端筛选、表格列与 CSV 字段，并优化桌面宽度下的表格可读性。

## 1.2.3 - 2026-07-21

- 清理未使用的 preload IPC、同步合并辅助函数、图标与样式定义，减少无效代码与重复规则。
- 新增跨平台源码换行约束并纳入 GitHub 公开清单，修正 MoonMeter 克隆目录和打包示例。

## 1.2.2 - 2026-07-21

- 统一仪表盘、API Key、余额查询与模型对比卡片的圆角、半透明表面和阴影层级，减少旧版品牌色渐变与“卡片套卡片”的视觉割裂。

## 1.2.1 - 2026-07-21

- 修复从 TokenLub/TokenScope 自动更新到 MoonMeter 后可能只有后台进程、没有主窗口的问题：正式版会在单实例锁和数据库初始化前继续使用拥有原始 `safeStorage` 加密上下文的旧用户目录，确保既有 API Key 与同步凭据可解密。
- 同步会话解密失败时不再阻断应用启动；MoonMeter 会正常显示窗口并将同步状态降级为需要重新登录，同时避免在日志和界面中暴露敏感密文。

## 1.2.0 - 2026-07-21

- 应用品牌升级为 MoonMeter，采用双月相标志、月光纸感浅深主题、Lucide 线性图标与克制数据动效。
- 桌面端新增浅色、深色和跟随系统三种外观模式，并完整适配所有页面、图表、表单与交互状态。
- 新增 `moonmeter://` 同步绑定协议和 `moonmeter.db`，同时保留旧 `tokenlub://`、TokenLub/TokenScope 数据库与本地排序设置的兼容迁移。
- 前端运行时升级到 React 19，安装包、窗口、快捷方式、导出文件与同步控制台统一使用 MoonMeter 名称。
- 重构模型对比页，以品牌 Logo 卡片展示费用排名、Provider、Token 构成、单次均值和计价覆盖，并限制为费用最高的前 12 个模型。
- 重构模型价格页，新增模型/Provider 搜索、来源/币种/计费区域筛选、目录状态摘要、最近变更概览和每页 50 条分页，同时将低频同步与汇率设置收进折叠区。

## 1.1.2 - 2026-07-20

- API Keys 与余额查询卡片新增可拖动排序，提供抬升、目标占位、邻卡让位和落位反馈；顺序按页面持久化，并支持筛选场景、方向键操作与 reduced motion。
- 完善模型价格匹配，对大小写、供应商前缀及 Kimi K3 别名进行规范化，避免已有目录价格因模型命名差异而无法命中。
- 项目用量统一在聚合前将多币种成本折算为人民币，避免不同币种直接相加导致费用统计失真。

## 1.1.1 - 2026-07-20

- 为全部 10 个桌面端页面加入与现有设计风格一致的语义化动效，覆盖数字、图表、进度、卡片、表格、状态和弹窗反馈。
- 新增统一 Motion tokens、数字递增、进度绘制和错峰分组组件，并限制位移、时长和批量动画数量，保持界面克制稳定。
- 完善 `prefers-reduced-motion` 降级、弹窗 ESC 关闭与焦点恢复，并新增真实 Electron 路由、动画收敛和可访问性测试。

## 1.1.0 - 2026-07-18

- 重构请求日志筛选区，使用供应商下拉、中文来源切换、日期范围和模型搜索统一定位日志，并移除重复的日期跳转操作。
- 优化请求日志表格，统一中文表头、供应商名称和来源文案，将币种合并到费用展示，并改善列宽、对齐与分页信息。
- Web 控制台新增账号与设备绑定记忆、登录会话恢复和显式退出；刷新页面后可安全续期会话，退出时同步撤销服务端会话。
- 完善控制台密码变更流程：改密成功后清除本地会话并要求重新登录，同时确保密码不会写入浏览器存储。

## 1.0.9 - 2026-07-18

- 重构 API Keys 卡片，以供应商品牌色和双列布局区分 API 余额、Coding Plan、Token 资源包、组织用量与聚合网关，并补充凭据类型、来源、Key 末位和查询状态。
- 升级订阅额度卡片，集中展示当前计划、登录状态、下次重置时间，以及 5 小时和周额度的进度与已用比例。
- 模型价格表新增人民币/美元统一显示切换，自动按每条价格的原始币种获取汇率并完成高精度折算，同时保留原始币种便于核对。
- 完善多币种汇率校验与异常提示；汇率不可用时不再展示误导性金额，并补充价格折算单元测试。

## 1.0.8 - 2026-07-17

- 简化项目用量热力图，改为按日期汇总的紧凑网格，并新增按项目着色的 Token 趋势曲线和入场动画。
- 重构余额查询卡片，明确区分 API 余额、Coding Plan、Token 资源包、组织用量、聚合网关和手动额度。
- 为 Kimi、MiniMax、LongCat、DeepSeek、GLM 等供应商增加差异化配色、核心指标、进度条和无数据说明。

## 1.0.7 - 2026-07-17

- 新增“异常价格变动需审批”开关，默认开启并持久化用户选择。
- 关闭审批后，待审批价格变更立即放行，后续官方目录同步也会直接应用异常价格变化。
- 手动同步官方价格会根据审批开关决定是否需要确认。

## 1.0.6 - 2026-07-17

- 修复本机 CLI Session 解析触发逻辑：打开 API Keys 页面不再自动解析。
- 自动解析仅在开关开启时由后台调度；手动解析仅通过“解析全部”或来源卡片的“解析入库”执行。
- Dashboard、Provider 汇总和请求日志的刷新操作不再隐式扫描 CLI 日志，关闭自动解析后状态也会持续保持。

## 1.0.5 - 2026-07-16

- 新增应用内检查更新与后台自动更新：从 GitHub Releases 获取当前系统对应版本，显示下载进度，下载完成后静默覆盖安装并重启。
- 更新只替换应用安装目录，SQLite 数据库与本地设置继续保存在 Electron 用户数据目录中。

## 1.0.4 - 2026-07-16

- 云端同步设置支持选择本地备份目录，预览会显示实际备份位置。
- 新增 Ubuntu 一键部署脚本与 Docker Compose 配置，支持 HTTPS 域名和 SSH 隧道两种同步服务模式。
- 增加服务器健康检查、备份、升级和卸载命令，并补充部署验收文档与自动化测试。
- 中文 README 作为默认项目首页，加入产品截图、功能展示和 GitHub Release 下载入口。

## 1.0.3 - 2026-07-15

- Web 控制台新增一键绑定桌面应用，通过 `tokenlub://` 唤起 TokenLub。
- 绑定过程使用两分钟有效且只能兑换一次的票据，不在链接中传递密码或会话令牌。
- 桌面端支持冷启动和已运行状态下接收绑定链接，并自动保存加密同步会话。

## 1.0.2 - 2026-07-14

- 加入设置、模型定价与余额快照的多设备云同步，支持持久化 outbox、分页拉取、bootstrap、冲突处理和设备撤销。
- 加入内置账号登录、设备与会话管理，以及无域名的 Ubuntu/Docker + SSH 隧道自托管部署方案。
- 补齐 PostgreSQL 迁移、历史清理、备份恢复、限流、隐私审计和双设备同步验证。

## 1.0.1 - 2026-07-10

- 正式切换 TokenLub 品牌资源和 Windows 安装产物命名。
- 余额查询页面的卡片头部改为复用 Provider logo，和 API Keys 页面保持一致。
- 清理旧版 TokenScope 安装包，补充中英文 README，并更新发布说明到 1.0.1。

## 1.0.0 - 2026-07-09

- 作为首个正式版本发布，整合用量总览、Provider 汇总、请求日志、价格配置、告警、API Keys 与本机 CLI Session 解析能力。
- 补齐请求日志的服务端筛选与全量 CSV 导出能力，并完成 Electron 打包链路整理，准备发布 Windows 安装包与便携版。

## 0.1.4 - 2026-07-09

- 在 0.1.3 的用量统计 UI 重构基础上，补齐请求日志分页能力，支持按页浏览与按日期跳转到当日请求列表。
- 修复左侧导航在不同页面下的对齐异常，统一图标、文字、角标与高亮底色的纵向节奏。
- Provider 汇总的每日费用趋势改为与用量概览一致的曲线图逻辑，按费用维度展示近 30 天成本波动，并修复原先空白不出图的问题。
- 用量概览的“当日”视图改为按小时展示 Token 消耗趋势，便于观察一天内的峰值分布。
- MiniMax Coding Plan 卡片将 `1.5h 限额` 更正为 `5h 限额`，并接入官方 `token_plan/remains` 配额读取，自动显示 5 小时与周限额。

## 0.1.3 - 2026-07-09

- API Keys 面板接入 LobeHub 模型/厂商 SVG 图标，DeepSeek、LongCat、MiniMax、Claude Code、Codex 等卡片会显示对应品牌标识。
- API Keys 的 Provider 筛选项增加小图标，缺少映射时使用稳定的首字母占位。
- 本机 Session 解析统一放入 API Keys 管理，并支持 Claude Code / Codex 用量卡片与自动解析开关。
- 用量概览支持当日、7 天、30 天、全部时间段，并使用多模型曲线图展示 Token 消耗。
- 消费统计统一按人民币展示，余额快照改为消费统计。
- API Key 卡片按不同 provider / coding plan 显示差异化信息，并增加编辑入口。
